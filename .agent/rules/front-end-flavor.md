---
trigger: always_on
---

For front-end code

proejct-level
1. use pnpm as package manager
2. use React
3. use jotai as React state management
4. use MUI as UI component library

typescript language flavor
1. use enum when possible